---
import Stars from '@components/Stars.astro';
import Layout from '@/Layout.astro';

const faq = [
    { a: 'A one-night festival in Austin where music videos take the spotlight — paired with live performances by nominee artists, a Q&A panel with directors, an awards ceremony, and a closing pool party. One night, one venue, every screen and stage active.', q: 'What exactly is the ATX MVFF?' },
    { a: 'Saturday, July 18, 2026, from 7 PM until 2 AM at Cabana Club in Austin, TX. Twelve programmed acts across the night, all under one roof.', q: 'When and where is it happening?' },
    { a: 'Submissions are open through FilmFreeway. Head to filmfreeway.com/atxmusicvideofilmfestival to enter — early-bird, regular, and final deadlines apply.', q: 'How do I submit a music video?' },
    { a: 'Yes — 21+ with valid ID. The bar is full-service and the pool party runs late.', q: 'Is there an age requirement?' },
    { a: 'Red carpet meets pool party. Dress to be photographed — and bring a swimsuit if you plan to take a dip after midnight.', q: 'What\'s the dress code?' },
    { a: 'Phones and small personal cameras are welcome. Professional press / accredited media — please reach out in advance via the contact form.', q: 'Can I bring a camera?' },
    { a: 'Street parking is available around the venue, but rideshare is strongly recommended given how late the event runs.', q: 'Where do I park?' },
    { a: 'Cabana Club\'s full-service bar is open all night. Light bites are served during the mixer; no outside food or drink permitted.', q: 'Will there be food and drinks?' },
];

const practical = [
    '21+ with valid government-issued ID — no exceptions',
    'Doors open at 7:00 PM, last entry at 11:30 PM',
    'No outside food or drink — full-service bar on site',
    'Pool access included with all ticket tiers — bring a suit',
    'Wristband re-entry permitted up until midnight',
    'Wheelchair-accessible venue — email us for any further accommodations',
];

const schedule = [
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
    ['TBD', 'Event TBD', 'TBD'],
];
---

<Layout
    description="Schedule, venue details, FAQ, and practical info for the ATX Music Video Film Festival — July 18, 2026."
    title="Info | ATX Music Video Film Festival"
>
    <Stars />
    <section class="section">
        <div class="shell">
            <div class="section__head">
                <span class="section__num">[ I ]</span>
                <h1 class="section__title">Info</h1>
                <span class="section__meta">SCHEDULE · VENUE<br />FAQ · PRACTICAL</span>
            </div>

            <div class="info__grid">
                <div>
                    <h2 class="subhead">Run of Show <span class="badge">7PM → 2AM</span></h2>
                    <div class="schedule flex flex-col">
                        {schedule.map(([time, event, tag]) => (
                            <div class="schedule__row">
                                <span class="schedule__time">{time}</span>
                                <span class="schedule__event">{event}</span>
                                <span class="schedule__tag">{tag}</span>
                            </div>
                        ))}
                    </div>
                </div>

                <div>
                    <h2 class="subhead">Venue <span class="badge">CAP 300</span></h2>
                    <div class="venue">
                        <div class="venue__name">Cabana Club</div>
                        <div class="venue__addr">AUSTIN · TEXAS</div>
                        <p class="venue__desc">Intimate venue, limited to 300 guests. Indoor screening room, outdoor pool area, full-service bar, and a dedicated VIP lounge. Street parking is available, but rideshare is strongly recommended given how late the night runs.</p>
                        <ul class="venue__features grid list-none">
                            <li>Indoor screen</li>
                            <li>Outdoor pool</li>
                            <li>Full bar</li>
                            <li>VIP lounge</li>
                            <li>Red carpet</li>
                            <li>ADA access</li>
                        </ul>
                    </div>

                    <h2 class="subhead mt-12">Practical <span class="badge">READ FIRST</span></h2>
                    <ul class="practical flex flex-col list-none">
                        {practical.map(p => (
                            <li>{p}</li>
                        ))}
                    </ul>
                </div>
            </div>

            <div class="info__faq">
                <h2 class="subhead">Frequently Asked <span class="badge">{faq.length} ENTRIES</span></h2>
                <div class="faq flex flex-col">
                    {faq.map((entry, i) => (
                        <div class={`faq__item ${i === 0 ? 'faq__item--open' : ''}`}>
                            <button
                                class="faq__q"
                                aria-expanded={i === 0 ? 'true' : 'false'}
                                type="button"
                            >
                                <span>{entry.q}</span>
                                <span class="faq__icon">+</span>
                            </button>
                            <div class="faq__a">{entry.a}</div>
                        </div>
                    ))}
                </div>
            </div>
        </div>
    </section>
</Layout>

<style>
    .info__grid {
        display: grid;
        gap: clamp(40px, 6vw, 80px);
        grid-template-columns: 1fr 1fr;
    }

    .schedule__row {
        align-items: baseline;
        border-bottom: 1px solid var(--color-cream-10);
        display: grid;
        gap: 18px;
        grid-template-columns: 100px 1fr auto;
        padding: 14px 0;
        position: relative;
        transition: padding 0.18s ease;
    }

    .schedule__row:hover {
        padding-left: 8px;
    }

    .schedule__row:hover .schedule__time {
        color: var(--color-accent);
    }

    .schedule__time {
        color: var(--color-cream);
        font-family: var(--font-mono);
        font-size: 13px;
        letter-spacing: 0.06em;
        transition: color 0.18s ease;
    }

    .schedule__event {
        font-family: var(--font-display);
        font-size: 18px;
        letter-spacing: 0.02em;
    }

    .schedule__tag {
        color: var(--color-cream-40);
        font-family: var(--font-mono);
        font-size: 9px;
        letter-spacing: 0.2em;
        text-transform: uppercase;
    }

    .venue {
        background: var(--color-navy);
        border: 1px solid var(--color-cream-15);
        margin-top: 16px;
        padding: 28px;
    }

    .venue__name {
        font-family: var(--font-display);
        font-size: 36px;
        line-height: 1;
    }

    .venue__addr {
        color: var(--color-cream-60);
        font-family: var(--font-mono);
        font-size: 12px;
        letter-spacing: 0.1em;
        margin-top: 6px;
    }

    .venue__desc {
        color: var(--color-cream-60);
        font-family: var(--font-serif);
        font-size: 15px;
        line-height: 1.5;
        margin-top: 14px;
    }

    .venue__features {
        gap: 8px;
        grid-template-columns: 1fr 1fr;
        margin-top: 18px;
    }

    .venue__features li {
        align-items: center;
        color: var(--color-cream);
        display: flex;
        font-family: var(--font-mono);
        font-size: 11px;
        gap: 8px;
        letter-spacing: 0.08em;
    }

    .venue__features li::before {
        color: var(--color-accent);
        content: '+';
        font-weight: 600;
    }

    .practical {
        gap: 10px;
    }

    .practical li {
        align-items: baseline;
        color: var(--color-cream);
        display: grid;
        font-family: var(--font-sans);
        font-size: 14px;
        gap: 12px;
        grid-template-columns: 24px 1fr;
    }

    .practical li::before {
        color: var(--color-accent);
        content: '►';
        font-size: 10px;
    }

    .info__faq {
        margin-top: clamp(60px, 8vw, 100px);
    }

    .faq__item {
        border-bottom: 1px solid var(--color-cream-15);
    }

    .faq__q {
        align-items: center;
        background: none;
        border: none;
        color: var(--color-cream);
        cursor: pointer;
        display: flex;
        font-family: var(--font-display);
        font-size: 20px;
        gap: 16px;
        justify-content: space-between;
        letter-spacing: 0.02em;
        padding: 18px 0;
        text-align: left;
        transition: color 0.18s ease;
        width: 100%;
    }

    .faq__q:hover {
        color: var(--color-accent);
    }

    .faq__icon {
        color: var(--color-accent);
        font-family: var(--font-mono);
        font-size: 20px;
        transition: transform 0.25s ease;
    }

    .faq__item--open .faq__icon {
        transform: rotate(45deg);
    }

    .faq__a {
        color: var(--color-cream-60);
        font-family: var(--font-serif);
        font-size: 15px;
        line-height: 1.55;
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.35s ease, padding 0.35s ease;
    }

    .faq__item--open .faq__a {
        max-height: 240px;
        padding: 0 0 20px;
    }

    @media (max-width: 1024px) {
        .info__grid {
            grid-template-columns: 1fr;
        }
    }
</style>

<script>
    let faqController: AbortController | undefined;

    function initFaq(): void {
        faqController?.abort();
        faqController = new AbortController();
        const { signal } = faqController;

        document.querySelectorAll('.faq__q').forEach((btn) => {
            btn.addEventListener('click', () => {
                const item = btn.closest('.faq__item') as HTMLElement | null;

                if (!item) return;

                const isOpen = item.classList.contains('faq__item--open');

                document.querySelectorAll('.faq__item--open').forEach((open) => {
                    open.classList.remove('faq__item--open');
                    open.querySelector('.faq__q')?.setAttribute('aria-expanded', 'false');
                });

                if (!isOpen) {
                    item.classList.add('faq__item--open');
                    btn.setAttribute('aria-expanded', 'true');
                }
            }, { signal });
        });
    }

    document.addEventListener('astro:page-load', initFaq);
</script>
