---

---

<div class="scroll-progress fixed" aria-hidden="true"></div>

<style>
    .scroll-progress {
        background: linear-gradient(90deg, var(--c-brand), var(--c-indigo), var(--c-cyan));
        height: 3px;
        left: 0;
        top: 0;
        transition: width var(--xs-4);
        width: 0%;
        z-index: 101;
    }
</style>
