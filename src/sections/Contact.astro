---
import SectionHeader from '@components/SectionHeader.astro';
import { LINKS } from '@lib/constants';
import { pad } from '@lib/utils';

const CHANNELS = [
    { href: LINKS.instagram, isExternal: true, isPrimary: false, label: 'Instagram', value: '@atxmvff' },
    { href: LINKS.email, isExternal: false, isPrimary: true, label: 'Email', value: 'contact@atxmusicvideofilmfestival.com' },
    { href: LINKS.phone, isExternal: false, isPrimary: false, label: 'Phone', value: '(281) 466-9387' },
    { href: LINKS.calendly, isExternal: true, isPrimary: false, label: 'Call', value: 'Calendly' },
] as const;
---

<section
    class="contact section"
    aria-labelledby="contact-title"
>
    <div class="shell">
        <SectionHeader headingId="contact-title">
            Contact
        </SectionHeader>
        <ol
            class="contact__channels list-none"
            data-scroll
            data-scroll-stagger="0.08"
        >
            {CHANNELS.map((channel, index) => (
                <li class:list={['contact__channel border-b border-cream-15', { 'contact__channel--primary': channel.isPrimary }]}>
                    <a
                        class="contact__channel-link grid isolate items-baseline relative no-underline text-cream"
                        href={channel.href}
                        rel={channel.isExternal ? 'noopener' : undefined}
                        target={channel.isExternal ? '_blank' : undefined}
                    >
                        <span class="contact__channel-index font-mono text-cream-60 select-none">/ {pad(index + 1)}</span>
                        <span class="contact__channel-label font-display uppercase">{channel.label}</span>
                        <span class="contact__channel-value flex items-baseline min-w-0 gap-2 font-serif italic wrap-anywhere text-cream-60">
                            <span class="min-w-0">{channel.value.split('@').flatMap((part, index) => (index === 0 ? [part] : [<wbr />, `@${part}`]))}</span>
                            {channel.isExternal && <span class="contact__channel-cue not-italic" aria-hidden="true">&rarr;</span>}
                        </span>
                    </a>
                </li>
            ))}
        </ol>
    </div>
</section>

<style>
    .contact__channel:last-child {
        border-bottom: none;
    }

    .contact__channel--primary .contact__channel-index {
        color: var(--color-coral);
    }

    .contact__channel--primary .contact__channel-label {
        color: var(--color-coral);
    }

    .contact__channel-index {
        font-size: 14px;
        letter-spacing: 0.16em;
    }

    .contact__channel-label {
        font-size: clamp(32px, calc(21.33px + 3.33vw), 64px);
        line-height: 0.95;
        transition:
            color var(--duration-fast) ease,
            transform var(--duration-fast) ease;
    }

    .contact__channel-link {
        gap: 4px clamp(16px, calc(8px + 2.5vw), 40px);
        grid-template-columns: 80px 1fr 1.2fr;
        padding: clamp(24px, calc(20px + 1.25vw), 36px) 0;
    }

    .contact__channel-link:active .contact__channel-label,
    .contact__channel-link:hover .contact__channel-label {
        color: var(--color-coral);
        transform: translateX(8px);
    }

    .contact__channel-link:active .contact__channel-value,
    .contact__channel-link:hover .contact__channel-value {
        color: var(--color-cream);
    }

    .contact__channel-link:active::after,
    .contact__channel-link:hover::after {
        opacity: 1;
    }

    .contact__channel-link::after {
        background: linear-gradient(90deg, transparent, var(--color-coral-10), transparent);
        content: '';
        inset: 0;
        opacity: 0;
        pointer-events: none;
        position: absolute;
        transition: opacity var(--duration-fast) ease;
        z-index: -1;
    }

    .contact__channel-value {
        font-size: clamp(20px, calc(18.67px + 0.42vw), 24px);
        justify-content: flex-end;
        line-height: 1.4;
        text-align: right;
        transition: color var(--duration-fast) ease;
    }

    @media (max-width: 768px) {
        .contact__channel-index {
            display: none;
        }

        .contact__channel-link {
            grid-template-columns: 1fr;
        }

        .contact__channel-value {
            justify-content: normal;
            text-align: left;
        }
    }
</style>
