---

---

<section
    class="tagline text-center"
    aria-label="Tagline"
    data-scroll
>
    <h2 class="tagline__main">
        <span class="tagline__a">Screens</span><span class="tagline__arrow"> → </span><span class="tagline__b">Stage</span>
    </h2>
    <p class="tagline__sub">An immersive production blending music and film, celebrating artists.</p>
</section>

<style>
    .tagline {
        padding: clamp(80px, 14vh, 160px) var(--shell-pad);
        position: relative;
    }

    .tagline__main {
        font-size: clamp(56px, 12vw, 180px);
        letter-spacing: -0.01em;
        line-height: 0.9;
        position: relative;
        text-transform: uppercase;
        z-index: 1;
    }

    .tagline__a {
        color: var(--color-cream);
    }

    .tagline__arrow {
        color: var(--color-accent);
        font-family: var(--font-serif);
        font-size: 0.7em;
        font-style: italic;
        padding: 0 0.15em;
    }

    .tagline__b {
        color: var(--color-accent);
        font-family: var(--font-serif);
        font-size: 0.92em;
        font-style: italic;
    }

    .tagline__sub {
        color: var(--color-cream-60);
        font-family: var(--font-serif);
        font-size: clamp(18px, 2.2vw, 26px);
        font-style: italic;
        line-height: 1.4;
        margin: 32px auto 0;
        max-width: 640px;
        position: relative;
        z-index: 2;
    }
</style>
